import React from 'react';

function Footer(){
  const footer = {
    height: '50px'
  }

  return (
    <div style={footer}>
        <h1>This is footer</h1>
    </div>
  );
}

export default Footer;
