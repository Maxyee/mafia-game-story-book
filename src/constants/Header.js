import React from 'react';

function Header(){

  const header = {
    //backgroundColor: 'black',
    height: '50px',
  }

  return (
    <div style={header}>
        <h1>This is header</h1>
    </div>
  );
}

export default Header;
