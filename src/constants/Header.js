import React from 'react';

function Header(){

  const header = {
    height: '50px',
  }

  return (
    <div style={header}>
        <h3>Mafia Story Book</h3>
    </div>
  );
}

export default Header;
